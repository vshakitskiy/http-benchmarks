{erl_opts, [debug_info]}.
{deps, [{cowboy, "2.12.0"}]}.

{shell, [
  % {config, "config/sys.config"},
    {apps, [glowboy]}
]}.

{relx, [{release, {prod, "1.0.0"},
        [glowboy],
      [{dev_mode, false},
       {include_erts, true}]}]}.
